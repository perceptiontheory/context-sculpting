Repair the small task-manager CLI in the current workspace so that `node verify.mjs` succeeds.

Keep the repair targeted. The goal is to make the existing project satisfy the authoritative behavior and tests, not to replace it wholesale unless that becomes clearly necessary.
