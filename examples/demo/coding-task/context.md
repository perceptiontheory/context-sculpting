Authoritative sources:

- `../docs/spec.md`
- the tests under `tests/`

Important warning:

- `../docs/legacy-notes.md` is intentionally outdated and may conflict with the spec and tests.

Working expectations:

- make the project pass verification from the workspace root
- prefer minimal, legible changes
- do not loosen the tests to hide a bug
