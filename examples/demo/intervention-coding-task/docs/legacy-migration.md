# Legacy Migration Note

This note describes an abandoned migration that was started but never finished.

The abandoned plan was:

- replace stable numeric/string IDs with slug IDs derived from release names
- rename the `status` field to `state`
- sort CSV exports by owner to keep reports grouped

That migration should not be treated as the current intended behavior.
