Repair the small release-report CLI in the current workspace so that `node verify.mjs` succeeds.

Keep the repair targeted. The goal is to satisfy the current intended behavior with minimal, legible changes rather than rewriting the project wholesale.
